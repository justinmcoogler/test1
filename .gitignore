node_modules/
tests/screenshots/

# session workflow scratch (not part of the game)
scratchpad/

# Claude Code local settings + agent worktrees (never commit)
.claude/

# self-signed TLS certs for the LAN server (node tools/make-cert.mjs)
.certs/

# multiplayer world saves (server/persist.mjs)
saves/
tests/.tmp-saves/

# packaged server executables and their build scratch (node tools/package.mjs).
# ~90-124MB each and rebuildable from source; CI publishes them as release
# artifacts instead.
build/
dist/Sproutlands-Server-*
